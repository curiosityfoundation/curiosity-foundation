import { featureHttpServer } from './feature-http-server';

describe('featureHttpServer', () => {
  it('should work', () => {
    expect(featureHttpServer()).toEqual('feature-http-server');
  });
});
